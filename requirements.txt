# Runtime dependencies (see pyproject.toml for the authoritative list).
# Install the project in editable mode instead for development:
#   pip install -e ".[dev]"
osmnx>=2.0
networkx>=3.2
rasterio>=1.3
numpy>=1.26
scikit-learn>=1.4
shapely>=2.0
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
